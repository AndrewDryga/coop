# Copy this file to `env` (same folder) and fill in any keys you use.
# Every value here is passed into the box for whichever agent you launch.
# Format is KEY=VALUE — no quotes, no `export`.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
