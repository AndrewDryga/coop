Reset this project's network approvals?

The project folder no longer exists.

This only resets the approvals; it does not change the settings in .agent/project.yaml.

To restore approval:
  coop net approve
