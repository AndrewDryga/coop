Review the requested network access changes.

NETWORK ACCESS
  Filtered — only approved network traffic is allowed.

NETWORK RULES
    github.com:443 · TLS          already approved
  + api.example.com:443 · TLS     new request
  - registry.npmjs.org:443 · TLS  no longer requested

